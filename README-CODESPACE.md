# 🚀 دليل تشغيل Saler CRM على GitHub Codespace

## 📋 **ما هو GitHub Codespace؟**
بيئة تطوير سحابية مُهيئة مسبقاً تعمل في المتصفح، تتكامل مع VS Code وتحتاج فقط اتصال بالإنترنت.

## 🎯 **المزايا:**
- ✅ لا حاجة لتثبيت أي شيء على الحاسوب
- ✅ بيئة تطوير جاهزة ومُعدة مسبقاً
- ✅ متوافق مع جميع أنظمة التشغيل
- ✅ مشاركة سهلة مع الفريق
- ✅ موارد حوسبة سحابية (4-8 CPU, 8-32GB RAM)

---

## 🚀 **طريقة التشغيل:**

### 1️⃣ **الخطوة الأولى: فتح Codespace**
```
1. اذهب إلى مستودع GitHub: https://github.com/XbBry/Saler.git
2. اضغط على زر "Code" (أخضر)
3. اختر "Open with Codespace"
4. انتظر حتى يتم إنشاء البيئة (2-3 دقائق)
```

### 2️⃣ **الخطوة الثانية: تشغيل النظام**
```bash
# تشغيل المشروع بالكامل (سحابة + قاعدة بيانات + واجهة)
docker-compose up -d

# أو تشغيل Frontend فقط
cd frontend && npm install && npm run dev

# أو تشغيل Backend فقط  
cd backend && pip install -r requirements.txt && uvicorn app.main:app --reload
```

### 3️⃣ **الخطوة الثالثة: الوصول للتطبيق**
```
🌐 Frontend: http://localhost:3000
🔧 Backend API: http://localhost:8000
📊 pgAdmin: http://localhost:8080
⚡ Redis: http://localhost:8081
```

---

## ⚙️ **التكوين المطلوب:**

### **الخدمات المُعدة تلقائياً:**
- **Node.js 18** (لـ Frontend)
- **Python 3.11** (لـ Backend)
- **Docker & Docker Compose**
- **PostgreSQL 15**
- **Redis 7**
- **Git**

### **المنافذ المفتوحة تلقائياً:**
- `3000` - Frontend (Next.js)
- `8000` - Backend (FastAPI)
- `8080` - pgAdmin
- `8081` - Redis Commander

---

## 🎯 **الاستخدام اليومي:**

### **بدء العمل اليومي:**
```bash
# تشغيل الخدمات
docker-compose up -d

# فحص الحالة
docker-compose ps

# متابعة السجلات
docker-compose logs -f
```

### **إيقاف العمل:**
```bash
# إيقاف الخدمات
docker-compose down

# أو إيقاف كامل مع البيانات
docker-compose down -v
```

---

## 📱 **الوصول من الجوال/التابلت:**
- **الطريقة:** استخدم رابط Codespace العام
- **المتصفح:** Safari (iOS) أو Chrome (Android)
- **الأداء:** ممتاز على الأجهزة الحديثة

---

## 🔧 **إدارة Codespace:**

### **إنشاء Codespace جديد:**
```bash
# حذف Codespace الحالي من GitHub
# ثم إنشاء واحد جديد من زر "Code"
```

### **تخصيص الموارد:**
- **Standard:** 4 CPU, 8GB RAM (مُناسب للمشاريع الصغيرة)
- **Large:** 8 CPU, 32GB RAM (للمشاريع الكبيرة)

### **المشاركة:**
```
1. في Codespace: اضغط "Share"
2. أرسل الرابط للعضو
3. سيحصل على وصول كامل للبيئة
```

---

## ⚡ **النصائح للحلول:**

### **بطء في التحميل:**
```bash
# تنظيف Docker وتحميل الصور من جديد
docker system prune -a
docker-compose pull
```

### **مشاكل في المنافذ:**
```bash
# فحص المنافذ المستخدمة
netstat -tulpn | grep :3000
netstat -tulpn | grep :8000
```

### **تحديث الكود:**
```bash
# في terminal جديد
git pull origin main
docker-compose down && docker-compose up -d
```

---

## 🎉 **مزايا Codespace للمشروع:**

### **للطوارئ:**
- ✅ **الوصول الفوري** بدون تثبيت
- ✅ **البيئة مُعدة مسبقاً** 100%
- ✅ **مشاركة سهلة** للعميل/الفريق

### **للتطوير:**
- ✅ **VS Code متكامل** مع Extensions
- ✅ **Terminal مدمج** لإدارة Docker
- ✅ **Git integration** تلقائي

### **للاختبار:**
- ✅ **بيئة معزولة** تماماً
- ✅ **موارد قابلة للتوسع**
- ✅ **أمان البيانات**

---

## 🚀 **ابدأ الآن:**
1. **اذهب إلى:** https://github.com/XbBry/Saler.git
2. **اضغط:** "Open with Codespace"
3. **انتظر:** تحميل البيئة (2-3 دقائق)
4. **تشغل:** `docker-compose up -d`
5. **استمتع:** بالتطوير من أي مكان! 🌟
