# دليل المساهمة في مشروع Saler

شكراً لاهتمامك بالمساهمة في مشروع Saler - منصة إدارة العملاء المحتملين بالذكاء الاصطناعي! هذا الدليل سيساعدك على البدء والتأكد من أن مساهمتك تتناسب مع معايير المشروع.

## 🚀 البدء السريع

### متطلبات النظام
- Python 3.11 أو أحدث
- Node.js 18 أو أحدث
- Docker و Docker Compose
- Git

### إعداد بيئة التطوير
```bash
# استنساخ المشروع
git clone https://github.com/YOUR_USERNAME/saler.git
cd saler

# إعداد Backend
cd backend
pip install -r requirements.txt
cp .env.example .env

# إعداد Frontend
cd ../frontend
npm install
cp .env.example .env.local

# تشغيل الخدمات (اختياري)
docker-compose -f docker-compose.dev.yml up -d
```

## 🤝 طرق المساهمة

### الإبلاغ عن الأخطاء
- استخدم [قالب تقرير الأخطاء](./ISSUE_TEMPLATE/bug_report.md)
- تأكد من أن الخطأ غير مذكور مسبقاً
- قدم خطوات لإعادة إنتاج الخطأ بوضوح

### طلب ميزات جديدة
- استخدم [قالب طلب الميزات](./ISSUE_TEMPLATE/feature_request.md)
- اشرح المشكلة التي تحلها الميزة
- اقترح واجهة المستخدم إذا كانت متعلقة بالـ frontend

### المساهمة في الكود
1.Fork المشروع
2.أنشئ فرع جديد للميزة/التصحيح
3.اتبع معايير الكود
4.اكتب اختبارات شاملة
5.تأكد من نجاح جميع الاختبارات

## 📋 معايير الكود

### Python (Backend)
```python
# استخدم docstrings باللغة العربية
def process_lead(lead_data: dict) -> Lead:
    """
    معالجة بيانات العميل المحتمل
    
    Args:
        lead_data: قاموس يحتوي على بيانات العميل
        
    Returns:
        Lead: كائن العميل المحتمل المعالج
        
    Raises:
        ValidationError: إذا كانت البيانات غير صحيحة
    """
    pass
```

- اتبع PEP 8 مع استثناء هذه النقاط:
  - استخدم 4 مسافات للـ indentation
  - استخدم double quotes للـ strings
  - أضف docstrings باللغة العربية
- استخدم type hints لجميع الدوال
- اكتب docstrings شاملة للدوال المعقدة

### TypeScript/React (Frontend)
```typescript
// استخدم تعليقات باللغة العربية
interface LeadFormData {
  name: string;
  email: string;
  phone?: string;
  source: string;
}

/**
 * مكون نموذج إضافة عميل محتمل جديد
 */
export const LeadForm: React.FC = () => {
  // كود المكون
};
```

- استخدم TypeScript لجميع الملفات
- اضبط ESLint و Prettier التلقائي
- استخدم Arabic naming في المتغيرات والوظائف عند الممكن
- اضبط المكونات لتعكس RTL text direction

### CSS/Styling
```css
/* استخدم BEM methodology */
.lead-card {
  /* أنماط أساسية */
}

.lead-card__title {
  /* عنوان البطاقة */
}

.lead-card__title--highlighted {
  /* عنوان مميز */
}

.lead-card--featured {
  /* بطاقة مميزة */
}
```

- استخدم Tailwind CSS للأطر الأساسية
- استخدم BEM naming methodology للـ custom styles
- اضبط الألوان لتعكس الهوية البصرية العربية

## 🧪 الاختبارات

### Backend Testing
```bash
# تشغيل جميع الاختبارات
pytest

# تشغيل مع coverage
pytest --cov=app

# تشغيل اختبارات محددة
pytest tests/test_leads.py
```

### Frontend Testing
```bash
# تشغيل جميع الاختبارات
npm test

# تشغيل مع coverage
npm run test:coverage

# اختبار مكونات محددة
npm test LeadForm
```

### متطلبات الاختبارات
- جميع الدوال والـ endpoints يجب أن تحتوي على اختبارات
- استخدم realistic test data
- اختبر edge cases والحالات الاستثنائية
- تحقق من رسائل الخطأ بالعربية

## 🔍 مراجعة الكود

### قبل إرسال Pull Request
- [ ] جميع الاختبارات تعمل بنجاح
- [ ] الكود يتبع معايير المشروع
- [ ] تم إضافة/تحديث الوثائق
- [ ] لا توجد warnings في TypeScript
- [ ] تم اختبار التغييرات يدوياً

### معايير PR
- **الوصف**: وصف واضح ومفصل للتغييرات
- **الفروع**: من feature_branch إلى develop
- **الاختبارات**: لا تطلب مراجعة بدون اختبارات
- **الوثائق**: حدث الوثائق إذا لزم الأمر

### نماذج Commit Messages
```
feat: إضافة نظام إشعارات العملاء المحتملين الجدد
fix: إصلاح خطأ عرض البيانات في لوحة التحكم
docs: تحديث دليل API للـ endpoints الجديدة
style: تحسين تنسيق نماذج العملاء المحتملين
refactor: إعادة هيكلة كود معالجة البيانات
test: إضافة اختبارات وحدة لمعالجة العملاء المحتملين
chore: تحديث dependencies لإصدار جديد
```

## 🌐 المعايير الثقافية واللغوية

### النص والواجهة
- جميع النصوص يجب أن تكون بالعربية
- استخدم language code `ar` في i18n
- اضبط التخطيط للـ RTL
- تأكد من دعم الخطوط العربية

### التوثيق
- README و Documentation بالعربية
- Code comments بالعربية للمهام المعقدة
- Error messages واضحة ومفيدة

## 📁 بنية الملفات

```
saler/
├── backend/
│   ├── app/
│   │   ├── api/          # API endpoints
│   │   ├── core/         # الإعدادات الأساسية
│   │   ├── models/       # نماذج البيانات
│   │   ├── schemas/      # Pydantic schemas
│   │   ├── services/     # منطق العمل
│   │   └── utils/        # أدوات مساعدة
│   ├── tests/            # اختبارات Python
│   └── requirements.txt
├── frontend/
│   ├── src/
│   │   ├── components/   # React components
│   │   ├── pages/        # صفحات التطبيق
│   │   ├── hooks/        # Custom hooks
│   │   ├── services/     # API calls
│   │   ├── utils/        # أدوات مساعدة
│   │   └── types/        # TypeScript types
│   ├── tests/            # اختبارات React
│   └── package.json
├── docs/                 # الوثائق
├── .github/              # GitHub templates و workflows
└── docker-compose.yml    # إعداد Docker
```

## 🐛 حل المشاكل الشائعة

### مشاكل البيئة
```bash
# إعادة بناء containers
docker-compose down && docker-compose up --build

# مسح cache Python
pip cache purge

# مسح cache Node.js
npm cache clean --force
```

### مشاكل التبعيات
```bash
# Backend
pip install -r requirements.txt --upgrade

# Frontend  
npm install && npm run build
```

## 📞 الحصول على المساعدة

### طرق التواصل
- **Issues**: للأسئلة التقنية وتقارير الأخطاء
- **Discussions**: للأسئلة العامة والاقتراحات
- **Email**: للدعم المباشر (saler@example.com)

### ساعات العمل
- الرد على Issues: خلال 24-48 ساعة
- مراجعات الكود: خلال 3-5 أيام عمل
- المشاكل الحرجة: فورياً

## 🎯 الأهداف القادمة

- [ ] تحسين أداء معالجة البيانات
- [ ] إضافة تحليلات متقدمة
- [ ] تطبيق AI للتحليل التنبؤي
- [ ] تحسين الأمان والحماية

---

## 🙏 شكراً لمساهمتك!

كل مساهمة، كبيرة كانت أم صغيرة، تساعد في جعل مشروع Saler أفضل. نحن نقدر وقتك وجهدك!

للمزيد من المعلومات، راجع:
- [دليل GitHub Actions](./README.md)
- [إعدادات البيئة](./environments/README.md)
- [معايير الأمان](./workflows/security.yml)