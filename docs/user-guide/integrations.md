# التكاملات - نظام Saler

## 🔗 مقدمة

التكاملات في نظام Saler تمكنك من ربط نظامك بالتطبيقات والخدمات الخارجية لتحسين كفاءة العمل وتبادل البيانات تلقائياً. هذا الدليل سيساعدك في إعداد وإدارة التكاملات بكفاءة.

---

## 🛍️ إعداد تكامل Shopify

### متطلبات البداية

1. **حساب Shopify**
   ```
   متطلبات:
   • حساب Shopify نشط (Basic Plan أو أعلى)
   • صلاحيات Admin أو Owner
   • مفتاح API من لوحة تحكم Shopify
   ```

2. **إعداد مفتاح API**
   ```
   المسار في Shopify:
   Settings → Apps and sales channels → Develop apps → Create an app
   
   الصلاحيات المطلوبة:
   ✓ Read customers
   ✓ Write customers
   ✓ Read products
   ✓ Read orders
   ✓ Write orders
   ✓ Read abandoned checkouts
   ```

### خطوات التكامل

1. **إنشاء التطبيق في Shopify**
   ```
   1. ادخل لوحة تحكم Shopify
   2. اذهب إلى Settings → Apps
   3. انقر "Manage private apps" أو "Develop apps"
   4. انقر "Create private app" أو "Create app"
   5. أدخل تفاصيل التطبيق:
      App name: Saler CRM Integration
      Emergency developer email: your@email.com
   ```

2. **ضبط الصلاحيات**
   ```
   Admin API permissions:
   
   Customers:
   • Read customers ✓
   • Write customers ✓
   
   Products:
   • Read products ✓
   
   Orders:
   • Read orders ✓
   • Write orders ✓
   
   Abandoned checkouts:
   • Read abandoned checkouts ✓
   
   Store information:
   • Read store information ✓
   ```

3. **الحصول على بيانات API**
   ```
   بعد حفظ الإعدادات:
   Admin API access token: [انسخ هذا]
   API key: [انسخ هذا]
   Password: [انسخ هذا للاستخدام في Saler]
   ```

4. **ربط بـ Saler**
   ```
   المسار في Saler:
   الإعدادات → التكاملات → Shopify
   
   أدخل البيانات:
   Store URL: https://your-shop.myshopify.com
   API Key: [من Shopify]
   Password: [من Shopify - اسمها "Admin API access token"]
   ```

### مزامنة البيانات

1. **مزامنة العملاء**
   ```
   عند التفعيل:
   • سيتم استيراد جميع عملاء Shopify
   • إنشاء عملاء جدد في Saler
   • تحديث معلومات العملاء الموجودين
   
   الحقول المزامنة:
   • الاسم الكامل
   • البريد الإلكتروني
   • رقم الهاتف
   • العنوان
   • تاريخ الإنشاء
   • مصدر الطلب
   ```

2. **مزامنة الطلبات**
   ```
   أنواع الطلبات المزامنة:
   • الطلبات المكتملة
   • الطلبات الملغاة
   • الطلبات المسترجعة
   • Checkouts المتروكة
   
   معلومات الطلبات:
   • رقم الطلب
   • تاريخ الطلب
   • حالة الطلب
   • إجمالي المبلغ
   • عناصر الطلب
   • بيانات العميل
   ```

3. **إعدادات المزامنة التلقائية**
   ```
   مزامنة فورية:
   ✅ طلبات جديدة
   ✅ تحديثات حالة الطلب
   ✅ عملاء جدد
   
   مزامنة دورية (كل ساعة):
   ✅ Checkouts المتروكة
   ✅ تحديثات العروض
   ✅ إلغاء الطلبات
   
   مزامنة يومية:
   ✅ تحليلات المبيعات
   ✅ إحصائيات شاملة
   ✅ تقارير الأداء
   ```

### استخدام البيانات المزامنة

1. **في لوحة التحكم**
   ```
   مؤشرات جديدة:
   • إجمالي مبيعات الشهر
   • متوسط قيمة الطلب
   • معدل التكرار في الشراء
   • أكثر المنتجات مبيعاً
   • أهم مناطق الشحن
   ```

2. **في إدارة العملاء**
   ```
   معلومات إضافية لكل عميل:
   • تاريخ آخر شراء
   • إجمالي المشتريات
   • عدد الطلبات
   • المنتجات المفضلة
   • حالة الاشتراك (إن وجدت)
   
   تصنيف العملاء:
   • VIP: مشتريات > 10,000 ريال
   • منتظم: 5+ طلبات
   • جديد: طلب واحد فقط
   • غير نشط: لم يطلب لأكثر من 6 أشهر
   ```

---

## 📱 إعداد تكامل Meta Ads

### متطلبات البداية

1. **حساب Meta Business**
   ```
   متطلبات:
   • حساب Meta Business Manager نشط
   • حساب إعلانات مدفوع
   • صلاحيات Admin في المعلن
   • تطبيق Facebook تم إنشاؤه
   ```

2. **إنشاء تطبيق Facebook**
   ```
   الخطوات:
   1. اذهب إلى developers.facebook.com
   2. انقر "Create App"
   3. اختر "Business"
   4. أدخل اسم التطبيق: "Saler CRM Integration"
   5. أضف Email للتواصل
   6. انقر "Create App ID"
   ```

### إعداد التطبيق

1. **إضافة Products**
   ```
   من لوحة التحكم:
   • Facebook Login: فعّل
   • Marketing API: فعّل
   • Webhooks: فعّل
   
   لكل منتج:
   1. انقر "Set Up"
   2. اتبع التعليمات
   3. احفظ App ID وApp Secret
   ```

2. **إعداد Facebook Login**
   ```
   Facebook Login → Settings:
   
   Valid OAuth Redirect URIs:
   https://your-saler-domain.com/auth/facebook/callback
   
   App Domains:
   your-saler-domain.com
   
   Site URL:
   https://your-saler-domain.com
   ```

3. **إعداد Marketing API**
   ```
   Marketing API → Tools → Graph API Explorer:
   
   اختر التطبيق
   اختر الإذونات:
   • ads_read
   • ads_management
   • business_management
   • pages_show_list
   
   احصل على User Access Token
   ```

### ربط المعلن

1. **إضافة المعلن إلى التطبيق**
   ```
   Marketing API → Settings:
   
   Add Ad Account:
   1. ادخل Ad Account ID (بدون act_前缀)
   2. اختر الأذونات المطلوبة
   3. أرسل طلب
    
   أو:
   1. اذهب إلى Business Settings
   2. Assets → Ad accounts
   3. أضف التطبيق كـ Partner
   4. امنح الأذونات المطلوبة
   ```

2. **ربط بـ Saler**
   ```
   المسار في Saler:
   الإعدادات → التكاملات → Meta Ads
   
   أدخل البيانات:
   App ID: [من Facebook]
   App Secret: [من Facebook]
   Access Token: [من Graph API Explorer]
   Ad Account ID: [بدون act_前缀]
   ```

### مزامنة بيانات الإعلانات

1. **البيانات المزامنة**
   ```
   إحصائيات الحملات:
   • الانطباعات (Impressions)
   • النقرات (Clicks)
   • تكلفة النقرة (CPC)
   • معدل التحويل (CTR)
   • التكلفة لكل اكتساب (CPA)
   
   البيانات المجمعة:
   • يومياً
   • لكل حملة
   • لكل مجموعة إعلانات
   • لكل إعلان
   ```

2. **تحليل الأداء**
   ```
   في Saler ستجد:
   
   تحليل العائد:
   • تكلفة اكتساب العميل من كل حملة
   • قيمة العميل المكتسب
   • عائد الاستثمار (ROI)
   
   تحسين الحملات:
   • الحملات الأكثر فعالية
   • الفئات المستهدفة الناجحة
   • أوقات الإعلانات الأمثل
   • التوصيات لتحسين الميزانية
   ```

3. **التكامل مع العملاء**
   ```
   عند ربط Meta Ads:
   • عملاء جدد من الإعلانات: تصنيف تلقائي
   • تتبع مسار العميل من الإعلان إلى الشراء
   • قياس فعالية كل إعلان في التحويل
   
   تصنيف العملاء:
   • عملاء إعلانات جوجل
   • عملاء فيسبوك
   • عملاء انستجرام
   • عملاء تكبير (إذا استخدم)
   ```

---

## 🌐 إدارة Webhooks

### فهم Webhooks

Webhooks هي طريقة للتواصل الفوري بين التطبيقات. عندما يحدث حدث معين في نظام خارجي (مثل Shopify أو Meta)، يتم إرسال إشعار فوري لـ Saler.

### إعداد Webhooks في Shopify

1. **إنشاء Webhook**
   ```
   في Shopify:
   Settings → Notifications → Webhooks
   
   انقر "Add webhook"
   
   اختر الحدث:
   • Order creation
   • Order cancellation
   • Order fulfilled
   • Customer creation
   • Checkout creation
   
   اختر Format: JSON
   اضبط URL: https://your-saler-domain.com/api/webhooks/shopify
   ```

2. **الأحداث المدعومة**
   ```
   أحداث الطلبات:
   ✓ order/create - إنشاء طلب جديد
   ✓ order/paid - تم الدفع
   ✓ order/fulfilled - تم الشحن
   ✓ order/cancelled - تم الإلغاء
   ✓ order/refunded - تم الاسترداد
   
   أحداث العملاء:
   ✓ customer/create - عميل جديد
   ✓ customer/update - تحديث بيانات العميل
   
   أحداث المتاجر:
   ✓ shop/update - تحديث معلومات المتجر
   ```

### إعداد Webhooks في Meta

1. **إعداد Webhook في Facebook**
   ```
   Developers Console:
   
   Webhooks → Subscribe
   
   App ID: [من التطبيق]
   Verify Token: [أدخل token مخصص]
   Callback URL: https://your-saler-domain.com/api/webhooks/facebook
   
   اختر الـ Subscriptions:
   ✓ ad_lead
   ✓ leadgen
   ✓ page
   ```

2. **إعدادات التطبيق**
   ```
   في الإعدادات:
   Webhooks → Configure
   
   Verify Token:
   أنشئ token آمن (مثال: "my_secure_webhook_token_2025")
   
   Callback URLs:
   https://your-saler-domain.com/api/webhooks/facebook
   https://your-saler-domain.com/api/webhooks/meta-ads
   ```

### معالجة البيانات الواردة

1. **تنسيق البيانات**
   ```
   مثال webhook من Shopify:
   {
     "id": 1234567890,
     "email": "customer@example.com",
     "phone": "+966501234567",
     "first_name": "أحمد",
     "last_name": "محمد",
     "orders_count": 2,
     "total_spent": "500.00",
     "created_at": "2025-11-01T10:00:00Z",
     "updated_at": "2025-11-01T15:30:00Z"
   }
   
   مثال webhook من Meta Ads:
   {
     "object": "ad_leadgen",
     "entry": [{
       "id": "1234567890",
       "changes": [{
         "field": "leadgen_event",
         "value": {
           "leadgen_id": "9876543210",
           "form_id": "1111111111",
           "page_id": "2222222222",
           "ad_id": "3333333333",
           "adgroup_id": "4444444444",
           "created_time": 1234567890
         }
       }]
     }]
   }
   ```

2. **معالجة الأحداث**
   ```
   في Saler:
   
   عند استقبال webhook من Shopify:
   1. التحقق من صحة الطلب
   2. فحص نوع الحدث
   3. معالجة البيانات
   4. تحديث/إنشاء العميل
   5. إرسال إشعار للفريق
   
   عند استقبال webhook من Meta:
   1. التحقق من token التحقق
   2. استخراج بيانات العميل الجديد
   3. إنشاء عميل في Saler
   4. تصنيف كمصدر "Meta Ads"
   5. إرسال رسالة ترحيب تلقائية
   ```

---

## 🔄 مزامنة البيانات

### أنواع المزامنة

1. **المزامنة الفورية (Real-time)**
   ```
   تحدث فوراً عند وقوع الحدث:
   
   مثال: عميل جديد في Shopify
   → إشعار webhook فوري لـ Saler
   → إنشاء عميل جديد خلال ثوانٍ
   → إرسال رسالة ترحيب تلقائية
   ```

2. **المزامنة المجدولة**
   ```
   تحدث في أوقات محددة:
   
   • كل 30 دقيقة: مزامنة الطلبات
   • كل ساعة: مزامنة إحصائيات الإعلانات
   • يومياً في 3:00 ص: مزامنة شاملة
   • أسبوعياً يوم الأحد: تقرير شامل
   ```

3. **المزامنة اليدوية**
   ```
   تتم عند الطلب:
   
   • مزامنة فورية لجميع البيانات
   • مزامنة نوع محدد (عملاء فقط)
   • مزامنة فترة زمنية محددة
   ```

### إدارة التعارضات

1. **استراتيجية حل التعارضات**
   ```
   قواعد الأولوية:
   
   1. آخر تعديل: البيانات الأحدث تفوز
   2. مصدر موثوق: Shopify > Meta > Saler يدوي
   3. إعدادات مخصصة: يمكن تخصيص الأولوية
   ```

2. **مثال عملي**
   ```
   العميل "أحمد محمد":
   
   في Shopify:
   البريد: ahmed@old-email.com
   
   في Saler:
   البريد: ahmed@new-email.com
   
   تم التحديث في: Saler (2:30 م)
   آخر تحديث في: Shopify (1:15 م)
   
   القرار: احتفظ ببيانات Saler (أحدث وقتاً)
   ```

### مراقبة المزامنة

1. **حالة المزامنة**
   ```
   في Saler ستجد:
   
   الحالة: 🟢 نشط
   آخر مزامنة: منذ 5 دقائق
   العناصر المزامنة اليوم:
   • 45 عميل جديد
   • 123 طلب
   • 67 تحويل من الإعلانات
   ```

2. **سجل الأخطاء**
   ```
   أخطاء المزامنة:
   
   ⚠️ تحذير - Shopify
   الطلب #123456: عميل غير موجود
   السبب: حذف العميل من Shopify
   الحل: إنشاء عميل جديد في Saler
   
   ❌ خطأ - Meta Ads
  Campaign #987654: فشل في المزامنة
   السبب: نفاد رصيد Access Token
   الحل: تحديث token
   ```

---

## 🔧 حل المشاكل

### مشاكل شائعة في Shopify

1. **خطأ في API**
   ```
   المشكلة:
   "Invalid API credentials"
   
   الحلول:
   1. تأكد من صحة API Key و Password
   2. تأكد من أن التطبيق مثبت في Shopify
   3. تحقق من انتهاء صلاحية token
   4. أعد إنشاء Access Token
   ```

2. **مشاكل الصلاحيات**
   ```
   المشكلة:
   "Insufficient permissions"
   
   الحلول:
   1. تحقق من الأذونات في Shopify
   2. تأكد من صلاحياتك كـ Admin
   3. أعد تثبيت التطبيق
   4. اتصل بدعم Shopify
   ```

3. **مشاكل المزامنة**
   ```
   المشكلة:
   "Sync timeout" أو "Partial sync"
   
   الحلول:
   1. تحقق من سرعة الإنترنت
   2. قلل حجم البيانات المزامنة
   3. غير أوقات المزامنة
   4. اتصل بالدعم الفني
   ```

### مشاكل شائعة في Meta Ads

1. **خطأ في Access Token**
   ```
   المشكلة:
   "Invalid access token"
   
   الحلول:
   1. تجديد Access Token
   2. تأكد من عدم انتهاء الصلاحية
   3. تحقق من صحة App ID
   4. أعد إعداد التطبيق
   ```

2. **مشاكل الأذونات**
   ```
   المشكلة:
   "Insufficient permissions for ads_read"
   
   الحلول:
   1. تأكد من صحة دورك في المعلن
   2. أضف التطبيق كـ Partner
   3. اطلب أذونات إضافية
   4. تحقق من Ad Account ID
   ```

### حلول عامة

| المشكلة | الحل السريع | الحل الدائم |
|---------|-------------|-------------|
| **لا تصل البيانات** | إعادة تشغيل التكامل | فحص إعدادات الخادم |
| **بيانات مكررة** | حذف التكرارات يدوياً | تفعيل تجميع التكرارات |
| **مزامنة بطيئة** | تقليل حجم البيانات | تحسين إعدادات الخادم |
| **رسائل خطأ** | إعادة المحاولة | فحص logs النظام |

---

## 📊 أفضل الممارسات

### أمان التكاملات

1. **حماية البيانات**
   ```
   أفضل الممارسات:
   • استخدم HTTPS دائماً
   • احتفظ بـ API Keys آمنة
   • غير الـ tokens دورياً
   • فعّل Webhook verification
   • راجع صلاحيات الوصول بانتظام
   ```

2. **المراقبة المستمرة**
   ```
   راقب:
   • عدد محاولات الدخول الفاشلة
   • حجم البيانات المنقولة
   • معدل الأخطاء في المزامنة
   • أوقات الاستجابة
   • استخدام الـ rate limits
   ```

### تحسين الأداء

1. **إدارة الموارد**
   ```
   تحسينات:
   • استخدم pagination للبيانات الكبيرة
   • فعّل caching للبيانات المتكررة
   • جدول المزامنة في أوقات هادئة
   • قلل عدد الـ API calls
   ```

2. **إدارة الأخطاء**
   ```
   استراتيجيات:
   • تنفيذ retry logic للـ API calls
   • استخدام exponential backoff
   • تسجيل الأخطاء للمراجعة
   • إرسال تنبيهات للأخطاء الحرجة
   ```

### التوثيق والصيانة

1. **توثيق التكاملات**
   ```
   احتفظ بسجلات:
   • تاريخ إعداد كل تكامل
   • التغييرات على الـ API keys
   • مشاكل وحلولها
   • تحديثات الروابط
   • إعدادات المزامنة
   ```

2. **الصيانة الدورية**
   ```
   مهام شهرية:
   • مراجعة أداء التكاملات
   • تحديث API endpoints
   • اختبار النسخ الاحتياطية
   • مراجعة صلاحيات الوصول
   • تحديث التوثيق
   ```

---

## ✅ قائمة التحقق النهائية

### إعداد Shopify

- [ ] ✅ إنشاء private app في Shopify
- [ ] ✅ الحصول على API credentials
- [ ] ✅ ربط التطبيق بـ Saler
- [ ] ✅ اختبار مزامنة العملاء
- [ ] ✅ اختبار مزامنة الطلبات
- [ ] ✅ إعداد webhook للطلبات الجديدة

### إعداد Meta Ads

- [ ] ✅ إنشاء Facebook App
- [ ] ✅ إضافة Marketing API
- [ ] ✅ الحصول على Access Token
- [ ] ✅ ربط المعلن بالتطبيق
- [ ] ✅ اختبار مزامنة بيانات الإعلانات
- [ ] ✅ إعداد webhook للعملاء الجدد

### إدارة Webhooks

- [ ] ✅ إعداد webhook URLs آمنة
- [ ] ✅ اختبار استقبال البيانات
- [ ] ✅ إعداد verification tokens
- [ ] ✅ اختبار معالجة البيانات
- [ ] ✅ مراقبة سجل الأخطاء

### المزامنة والأمان

- [ ] ✅ اختبار المزامنة الفورية
- [ ] ✅ اختبار المزامنة المجدولة
- [ ] ✅ إعداد مراقبة الأداء
- [ ] ✅ تفعيل النسخ الاحتياطية
- [ ] ✅ توثيق جميع الإعدادات
- [ ] ✅ تدريب الفريق على الإدارة

---

## 🚀 الخطوات التالية

بعد إتقان التكاملات:

1. [**الذكاء الاصطناعي**](./ai-features.md) - لاستخدام التحليلات المتقدمة
2. [**الإعدادات المتقدمة**](./advanced-settings.md) - لتخصيص النظام
3. [**الأسئلة الشائعة**](./faq.md) - لحل المشاكل الشائعة

---

## 📞 الدعم والمساعدة

### مشاكل تقنية

**Shopify API محدود:**
- تحقق من حدود الاستخدام في Shopify
- قلل عدد المزامنات اليومية
- استخدم pagination للبيانات الكبيرة

**Meta Ads Token ينتهي:**
- اضبط تجديد تلقائي للـ tokens
- احتفظ بـ backup tokens
- راقب تواريخ انتهاء الصلاحية

**Webhooks لا تصل:**
- تحقق من صحة URLs
- تأكد من تفعيل Webhooks في المصدر
- راجع firewall وإعدادات الخادم

**مزامنة بطيئة:**
- قلل حجم البيانات المزامنة
- غير أوقات المزامنة
- راجع موارد الخادم

**تواصل معنا:**
- البريد: support@saler.com
- الدردشة: متاحة 24/7
- الهاتف: +966xxxxxxxxx
- التوثيق الفني: docs.saler.com/integrations

---

*آخر تحديث: نوفمبر 2025*