# 🚀 دليل Makefile الشامل - Saler SaaS Platform

## نظرة عامة

هذا الدليل يشرح استخدام Makefile الشامل لمنصة Saler SaaS، والذي يوفر أكثر من 100 أمر مختلف لإدارة المشروع بكفاءة عالية.

## 📋 المحتويات

- [الأوامر الأساسية](#الأوامر-الأساسية)
- [أوامر التطوير](#أوامر-التطوير)
- [أوامر Docker والحاويات](#أوامر-docker-والحاويات)
- [أوامر الخادم الخلفي](#أوامر-الخادم-الخلفي)
- [أوامر واجهة المستخدم](#أوامر-واجهة-المستخدم)
- [أوامر قاعدة البيانات](#أوامر-قاعدة-البيانات)
- [أوامر الاختبارات](#أوامر-الاختبارات)
- [أوامر المراقبة](#أوامر-المراقبة)
- [أوامر النشر](#أوامر-النشر)
- [أوامر الأمان](#أوامر-الأمان)
- [أوامر الصيانة](#أوامر-الصيانة)
- [أمثلة الاستخدام المتقدم](#أمثلة-الاستخدام-المتقدم)

---

## 🎯 الأوامر الأساسية

### عرض المساعدة
```bash
make help                    # عرض جميع الأوامر المتاحة
make info                    # عرض معلومات مفصلة عن النظام
make status                  # عرض حالة النظام الحالية
make version                 # عرض إصدار المشروع
```

### التنظيف والصيانة
```bash
make clean                   # تنظيف الملفات المؤقتة
make clean-all              # تنظيف شامل مع الحاويات
make reset                  # إعادة تعيين البيئة بالكامل
```

---

## 🛠️ أوامر التطوير

### الإعداد الأولي
```bash
make setup                  # إعداد كامل للمشروع
make setup-backend          # إعداد البيئة الخلفية
make setup-frontend         # إعداد واجهة المستخدم
make setup-db               # إعداد قاعدة البيانات
make setup-env              # إعداد ملفات المتغيرات
```

### تشغيل بيئة التطوير
```bash
make dev                    # تشغيل جميع خدمات التطوير
make dev-backend           # تشغيل الخادم الخلفي فقط
make dev-frontend          # تشغيل واجهة المستخدم فقط
```

### أوامر البدء والإيقاف
```bash
make start                 # synonym for up
make stop                  # synonym for down
make restart               # إعادة تشغيل الخدمات
```

---

## 🐳 أوامر Docker والحاويات

### بناء الصور
```bash
make docker-build          # بناء جميع الصور
make docker-build-prod     # بناء صور الإنتاج
```

### إدارة الخدمات
```bash
make up                    # تشغيل جميع الخدمات
make down                  # إيقاف جميع الخدمات
make restart               # إعادة تشغيل الخدمات
```

### السجلات والمراقبة
```bash
make logs                  # عرض سجلات جميع الخدمات
make logs-backend          # عرض سجلات الخادم الخلفي
make logs-frontend         # عرض سجلات واجهة المستخدم
make logs-db              # عرض سجلات قاعدة البيانات
make docker-ps            # عرض قائمة الحاويات
make docker-stats         # عرض إحصائيات الموارد
```

### الوصول للحاويات
```bash
make exec                  # الدخول إلى حاوية الخدمة
make shell                 # synonym for exec
```

### التنظيف
```bash
make docker-clean          # تنظيف موارد Docker
```

---

## 🔧 أوامر الخادم الخلفي

### التشغيل والبناء
```bash
make backend-run           # تشغيل الخادم الخلفي
make backend-build         # بناء الخادم الخلفي
```

### الاختبارات
```bash
make backend-test          # تشغيل اختبارات الخادم الخلفي
make backend-test-unit     # تشغيل اختبارات وحدة الخادم الخلفي
make backend-test-integration  # تشغيل اختبارات التكامل
```

### فحص جودة الكود
```bash
make backend-lint          # فحص جودة كود الخادم الخلفي
make backend-format        # تنسيق كود الخادم الخلفي
```

### قاعدة البيانات
```bash
make backend-migrate       # تشغيل هجرات قاعدة البيانات
```

### أدوات التطوير
```bash
make backend-shell         # فتح shell في الخادم الخلفي
make backend-docs          # عرض وثائق API
```

---

## 🎨 أوامر واجهة المستخدم

### التشغيل والبناء
```bash
make frontend-run          # تشغيل واجهة المستخدم
make frontend-build        # بناء واجهة المستخدم
```

### الاختبارات
```bash
make frontend-test         # تشغيل اختبارات واجهة المستخدم
```

### فحص جودة الكود
```bash
make frontend-lint         # فحص جودة كود واجهة المستخدم
make frontend-format       # تنسيق كود واجهة المستخدم
```

### فحوصات خاصة
```bash
make frontend-type-check   # فحص أنواع TypeScript
make frontend-optimize     # تحسين واجهة المستخدم
make frontend-zod-check    # فحص مخططات Zod
```

---

## 💾 أوامر قاعدة البيانات

### إدارة البيانات
```bash
make db-create             # إنشاء قاعدة بيانات جديدة
make db-drop              # حذف قاعدة البيانات
make db-reset             # إعادة تعيين قاعدة البيانات
```

### النسخ الاحتياطية
```bash
make db-backup            # إنشاء نسخة احتياطية من قاعدة البيانات
make db-restore           # استعادة قاعدة البيانات من نسخة احتياطية
```

### الهجرات
```bash
make db-migrate           # تشغيل هجرات قاعدة البيانات
make migrate-latest       # تطبيق أحدث هجرات
make migrate-rollback     # التراجع عن هجرة
make migrate-generate     # إنشاء هجرة جديدة
```

### الوصول والمراقبة
```bash
make db-shell             # فتح shell قاعدة البيانات
make db-status            # عرض حالة قاعدة البيانات
make db-seed              # إدراج بيانات تجريبية
```

---

## 🧪 أوامر الاختبارات

### الاختبارات الشاملة
```bash
make test                 # تشغيل جميع الاختبارات
make test-unit           # تشغيل اختبارات الوحدة
make test-integration    # تشغيل اختبارات التكامل
make test-e2e           # تشغيل اختبارات end-to-end
```

### اختبارات متقدمة
```bash
make test-coverage       # تشغيل اختبارات مع تقرير التغطية
make test-watch         # تشغيل اختبارات في وضع المراقبة
make test-parallel      # تشغيل اختبارات متوازية
```

---

## 📊 أوامر المراقبة

### أدوات المراقبة
```bash
make monitor-up          # تشغيل أدوات المراقبة
make monitor-down        # إيقاف أدوات المراقبة
```

### فحص الصحة والأداء
```bash
make health-check        # فحص صحة النظام
make metrics             # عرض مقاييس النظام
make performance-check   # فحص أداء النظام
```

### تحليل السجلات
```bash
make logs-analyze        # تحليل سجلات النظام
```

---

## 🚀 أوامر النشر

### نشر بيئات مختلفة
```bash
make deploy-staging      # نشر على بيئة التجهيز
make deploy-prod         # نشر على بيئة الإنتاج
make deploy-rollback     # التراجع عن نشر
```

### Kubernetes
```bash
make k8s-deploy          # نشر على Kubernetes
make k8s-rollback        # التراجع عن نشر Kubernetes
make k8s-status          # عرض حالة نشر Kubernetes
```

---

## 🔒 أوامر الأمان

### الفحوصات الأمنية
```bash
make security-audit      # فحص أمني شامل
make security-scan       # فحص ثغرات الأمان
make security-update     # تحديث التبعيات الأمنية
```

---

## 🔧 أوامر الصيانة

### النسخ الاحتياطية والاستعادة
```bash
make backup              # إنشاء نسخة احتياطية شاملة
make restore             # استعادة من نسخة احتياطية
```

### الصيانة
```bash
make maintenance-start   # بدء وضع الصيانة
make maintenance-end     # إنهاء وضع الصيانة
```

### التحديثات
```bash
make update              # تحديث النظام
make check-updates       # فحص التحديثات المتاحة
```

---

## 📚 أوامر الوثائق

```bash
make docs                # إنشاء الوثائق
make docs-serve          # تشغيل خادم الوثائق
make api-docs           # عرض وثائق API
```

---

## 🏭 أوامر متقدمة

### اختبارات الأداء
```bash
make benchmark           # تشغيل اختبارات الأداء
make load-test          # تشغيل اختبارات الحمولة
make smoke-test         # تشغيل اختبارات الدخان
make profile            # تشغيل profiler
```

### وضع Debug
```bash
make debug               # تشغيل في وضع Debug
```

---

## 🔄 أوامر CI/CD

### إعداد بيئة CI/CD
```bash
make ci-setup            # إعداد بيئة CI/CD
make ci-test             # تشغيل اختبارات CI
make ci-build            # بناء CI
```

### الإصدارات
```bash
make release             # إنشاء إصدار جديد
```

---

## 🎭 أوامر إضافية

### اختصارات شائعة
```bash
make up-dev              # synonym for dev
make down-all            # synonym for down
make restart-all         # synonym for restart
make test-all            # synonym for test
make lint-all           # فحص جودة جميع الأكواد
make build-all          # بناء جميع المكونات
make check-all          # فحص شامل
```

### اختصارات الوصول
```bash
make shell-backend      # synonym for backend-shell
make shell-frontend     # synonym for exec
make shell-db          # synonym for db-shell
```

### اختصارات سريعة
```bash
make q                  # synonym for quick
make quick              # فحص صحة سريع
```

---

## 📖 أمثلة الاستخدام المتقدم

### سير العمل اليومي للتطوير
```bash
# البدء
make setup              # إعداد أولي للمشروع
make dev                # تشغيل بيئة التطوير

# أثناء التطوير
make test               # تشغيل الاختبارات
make backend-lint       # فحص جودة الكود
make clean              # تنظيف الملفات المؤقتة

# المراقبة
make health-check       # فحص صحة النظام
make logs-backend      # مراجعة سجلات الخادم الخلفي
```

### سير العمل للنشر على الإنتاج
```bash
# التحضير
make validate-env       # التحقق من متغيرات البيئة
make security-audit     # فحص أمني شامل
make backup             # إنشاء نسخة احتياطية

# البناء والنشر
make docker-build-prod  # بناء صور الإنتاج
make deploy-prod        # نشر على الإنتاج
make health-check      # فحص صحة ما بعد النشر

# المراقبة
make monitor-up        # تشغيل أدوات المراقبة
```

### إدارة قاعدة البيانات
```bash
# الإعداد الأولي
make setup-db          # إعداد قاعدة البيانات
make migrate-latest    # تطبيق أحدث هجرات
make db-seed           # إدراج بيانات تجريبية

# إدارة البيانات
make db-backup         # إنشاء نسخة احتياطية
make db-migrate        # تشغيل هجرات جديدة
make migrate-generate "Add new table"  # إنشاء هجرة جديدة

# حل المشاكل
make db-status         # فحص حالة قاعدة البيانات
make db-shell          # الدخول إلى shell قاعدة البيانات
```

### الصيانة الدورية
```bash
# التنظيف الدوري
make clean             # تنظيف الملفات المؤقتة
make docker-clean      # تنظيف موارد Docker

# التحديثات
make check-updates     # فحص التحديثات المتاحة
make update            # تحديث النظام

# المراقبة
make metrics           # عرض مقاييس النظام
make logs-analyze      # تحليل السجلات
```

---

## 🔧 إعدادات متقدمة

### استخدام متغيرات البيئة
```bash
# تحديد البيئة
make ENV=staging dev

# تحديد عدد المعالجات المتوازية
make JOBS=8 test-parallel

# تحديد كلمة مرور قاعدة البيانات
make DB_PASSWORD=securepass setup-db

# تحديد Docker registry
make REGISTRY=my-registry.com docker-build
```

### أوامر مخصصة للتطوير
```bash
# تطوير سريع
make dev               # تشغيل سريع للتطوير
make test-watch        # مراقبة الاختبارات
make debug             # تشغيل مع debugger

# بناء وتحسين
make frontend-optimize # تحسين واجهة المستخدم
make backend-format    # تنسيق الكود
```

---

## 🚨 نصائح مهمة

### الأمان
1. **لا تشارك مفاتيح API**: استخدم متغيرات البيئة
2. **فحص أمني دوري**: استخدم `make security-audit`
3. **النسخ الاحتياطية**: اعمل نسخ احتياطية منتظمة

### الأداء
1. **استخدم الاختبارات المتوازية**: `make test-parallel`
2. **راقب الموارد**: `make metrics`
3. **نظف الملفات المؤقتة**: `make clean`

### الصيانة
1. **فحص التحديثات**: `make check-updates`
2. **تحديث دوري**: `make update`
3. **مراقبة السجلات**: `make logs-analyze`

---

## 📞 الدعم والمساعدة

- **عرض الأوامر**: `make help`
- **معلومات النظام**: `make info`
- **حالة النظام**: `make status`
- **فحص شامل**: `make health-check`

---

## 📝 ملاحظات إضافية

### متطلبات النظام
- Docker & Docker Compose
- Python 3.11+
- Node.js 18+
- PostgreSQL
- Redis

### ملاحظات الأداء
- استخدم `make test-parallel` للاختبارات السريعة
- استخدم `make monitor-up` للمراقبة المستمرة
- استخدم `make logs-analyze` لتحليل السجلات

### أفضل الممارسات
1. عمل `make backup` قبل أي تغيير كبير
2. تشغيل `make health-check` بانتظام
3. استخدام `make security-audit` دورياً
4. مراجعة السجلات بـ `make logs-analyze`

---

*تم إنشاء هذا الدليل تلقائياً بناءً على Makefile الشامل*