# 🚀 Makefile - دليل سريع للمطورين

## نظرة عامة

Makefile شامل ومتقدم لمنصة Saler SaaS يوفر أكثر من **100 أمر** مختلف لإدارة المشروع بكفاءة عالية.

## 🎯 البدء السريع

### 1. عرض جميع الأوامر
```bash
make help
```

### 2. الإعداد الأولي
```bash
# إعداد كامل للمشروع
make setup

# أو استخدم السكريبت السريع
bash scripts/quick-init.sh
```

### 3. بدء التطوير
```bash
# تشغيل جميع خدمات التطوير
make dev

# أو تشغيل الخدمات مع واجهات المراقبة
make up
```

## 📋 الأوامر الأساسية

| الفئة | الأوامر المهمة |
|-------|----------------|
| **التطوير** | `make dev`, `make setup`, `make reset` |
| **Docker** | `make up`, `make down`, `make logs`, `make docker-build` |
| **الخادم الخلفي** | `make backend-run`, `make backend-test`, `make backend-lint` |
| **واجهة المستخدم** | `make frontend-run`, `make frontend-build`, `make frontend-test` |
| **قاعدة البيانات** | `make db-migrate`, `make db-backup`, `make db-restore` |
| **الاختبارات** | `make test`, `make test-coverage`, `make test-parallel` |
| **المراقبة** | `make health-check`, `make metrics`, `make monitor-up` |
| **النشر** | `make deploy-staging`, `make deploy-prod` |
| **الأمان** | `make security-audit`, `make security-scan` |

## 🛠️ سير العمل اليومي

### بدء يوم العمل
```bash
# فحص حالة النظام
make status

# بدء بيئة التطوير
make dev

# تشغيل الاختبارات
make test
```

### أثناء التطوير
```bash
# فحص جودة الكود
make lint-all

# تنسيق الكود
make backend-format
make frontend-format

# تشغيل اختبارات سريعة
make test-parallel

# فحص صحة النظام
make health-check
```

### إنهاء يوم العمل
```bash
# إنشاء نسخة احتياطية
make backup

# إيقاف الخدمات
make down

# تنظيف الملفات المؤقتة
make clean
```

## 🔧 الفئات الرئيسية

### 1. أوامر التطوير 🚀
- `make setup` - إعداد كامل للمشروع
- `make dev` - بدء بيئة التطوير
- `make reset` - إعادة تعيين البيئة
- `make clean` - تنظيف الملفات المؤقتة

### 2. أوامر Docker 🐳
- `make docker-build` - بناء جميع الصور
- `make up` - تشغيل الخدمات
- `make down` - إيقاف الخدمات
- `make logs` - عرض السجلات
- `make exec` - الدخول للحاويات

### 3. أوامر الخادم الخلفي 🔧
- `make backend-run` - تشغيل الخادم
- `make backend-test` - تشغيل الاختبارات
- `make backend-lint` - فحص جودة الكود
- `make backend-migrate` - تشغيل الهجرات

### 4. أوامر واجهة المستخدم 🎨
- `make frontend-run` - تشغيل الفرونت إند
- `make frontend-build` - بناء الفرونت إند
- `make frontend-test` - تشغيل اختبارات الفرونت إند
- `make frontend-lint` - فحص جودة كود الفرونت إند

### 5. أوامر قاعدة البيانات 💾
- `make db-create` - إنشاء قاعدة البيانات
- `make db-migrate` - تشغيل الهجرات
- `make db-backup` - إنشاء نسخة احتياطية
- `make db-restore` - استعادة من نسخة احتياطية

### 6. أوامر الاختبارات 🧪
- `make test` - تشغيل جميع الاختبارات
- `make test-unit` - اختبارات الوحدة
- `make test-integration` - اختبارات التكامل
- `make test-coverage` - اختبارات مع التغطية
- `make test-parallel` - اختبارات متوازية

### 7. أوامر المراقبة 📊
- `make health-check` - فحص صحة النظام
- `make metrics` - عرض المقاييس
- `make monitor-up` - تشغيل أدوات المراقبة
- `make logs-analyze` - تحليل السجلات

### 8. أوامر النشر 🚀
- `make deploy-staging` - نشر على التجهيز
- `make deploy-prod` - نشر على الإنتاج
- `make k8s-deploy` - نشر على Kubernetes

### 9. أوامر الأمان 🔒
- `make security-audit` - فحص أمني شامل
- `make security-scan` - فحص الثغرات
- `make security-update` - تحديث التبعيات الأمنية

## 🎮 أمثلة متقدمة

### نشر سريع على الإنتاج
```bash
make ENV=production deploy-prod
make health-check
make backup
```

### اختبار شامل
```bash
make test-coverage
make security-audit
make performance-check
```

### تنظيف شامل وإعادة تشغيل
```bash
make clean-all
make reset
make dev
```

### مراقبة متقدمة
```bash
make monitor-up
make health-check
make logs-analyze
make metrics
```

## 🔧 متغيرات البيئة

### متغيرات مفيدة
```bash
# تحديد البيئة
make ENV=staging dev

# تحديد كلمة مرور قاعدة البيانات
make DB_PASSWORD=securepass setup-db

# تحديد عدد المعالجات المتوازية
make JOBS=8 test-parallel

# تحديد Docker registry
make REGISTRY=my-registry.com docker-build
```

## 📊 الأوامر حسب الهدف

### للتطوير اليومي
- `make dev` - البدء
- `make test` - الاختبار
- `make health-check` - المراقبة
- `make logs` - المراجعة

### للنشر
- `make deploy-staging` - تجهيز
- `make deploy-prod` - إنتاج
- `make health-check` - فحص
- `make monitor-up` - مراقبة

### للصيانة
- `make backup` - نسخ احتياطية
- `make update` - تحديثات
- `make clean` - تنظيف
- `make security-audit` - أمان

## 🚨 نصائح مهمة

### الأداء
1. استخدم `make test-parallel` للاختبارات السريعة
2. استخدم `make health-check` للمراقبة المستمرة
3. استخدم `make logs-analyze` لتحليل السجلات

### الأمان
1. استخدم `make security-audit` دورياً
2. احفظ النسخ الاحتياطية بـ `make backup`
3. تحقق من صحة النظام بـ `make status`

### أفضل الممارسات
1. ابدأ بيومك بـ `make status`
2. استخدم `make help` عند الحاجة
3. احتفظ ببيئة نظيفة بـ `make clean`

## 📞 الدعم السريع

### أوامر مساعدة سريعة
```bash
make help           # عرض جميع الأوامر
make info           # معلومات النظام
make status         # حالة النظام
make version        # إصدار المشروع
```

### فحص سريع
```bash
make health-check   # فحص صحة سريع
make quick          # فحص سريع (synonym)
```

### أدوات مفيدة
```bash
scripts/validate-setup.sh  # فحص شامل للنظام
scripts/quick-init.sh      # تهيئة سريعة
```

## 🔗 روابط مفيدة

- [دليل Makefile الشامل](MAKEFILE_GUIDE.md)
- [README الرئيسي](README.md)
- [وثائق المشروع](docs/)

---

## 📝 ملاحظات

- هذا Makefile يدعم أكثر من **100 أمر** مختلف
- جميع الأوامر تدعم اللغة العربية
- يتم حفظ السجلات في مجلد `logs/`
- النسخ الاحتياطية تحفظ في مجلد `backups/`
- يستخدم ألوان ورموز تعبيرية لسهولة القراءة

**استمتع بالتطوير مع Makefile! 🚀**